

一、安装：

	1、运行ReplaceText.mxp(或ReplaceText.zxp)，按提示步骤安装。

二、功能及作用：

	1、在当前打开的fla文件中遍历查找并替换指定的文本字符。

三、使用：

	1、成功安装后在Flash Professional菜单中，
	选择“窗口”→“其他面板”→“ReplaceText”，即可打开功能面板。
	
	2、分别在“查找文字”和“替换为”的输入区域输入字符，点击“查找并替换”
	按钮即可将当前文件库中的所有元件中的文字替换(静态或动态文本均可)。